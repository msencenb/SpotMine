				<%= form_for(@spot_listing) do |f| %>
				  <% if @spot_listing.errors.any? %>
				    <div id="error_explanation">
				      <h2><%= pluralize(@spot_listing.errors.count, "error") %> prohibited this spot_listing from being saved:</h2>

				      <ul>
				      <% @spot_listing.errors.full_messages.each do |msg| %>
				        <li><%= msg %></li>
				      <% end %>
				      </ul>
				    </div>
				  <% end %>

				  <div class="field">
				    <%= f.label :title %><br />
				    <%= f.text_area :title %>
				  </div>
				  <div class="field">
				    <%= f.label :description %><br />
				    <%= f.text_area :description %>
				  </div>
				  <div class="field">
				    <%= f.label :amount %><br />
				    <%= f.text_field :amount %>
				  </div>
				  <div class="actions">
				    <%= f.submit %>
				  </div>
				<% end %>
